---
phase: 03-transcript-persistence-and-inspection
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/paths.ts
  - src/debate/transcript.ts
  - tests/debate/run-debate.test.ts
autonomous: true
requirements:
  - TRNS-01
must_haves:
  truths:
    - "The codebase has one stable structured transcript record shape for a complete debate run."
    - "Transcript storage includes explicit stage order, run metadata, prompt context, output content, and debug metadata."
    - "Transcript files are addressable by run id in a predictable app-local storage directory."
  artifacts:
    - path: "src/debate/transcript.ts"
      provides: "Typed transcript schema and file IO helpers for one JSON record per debate run"
      exports: ["DebateTranscriptRecord", "createDebateTranscriptRecord", "saveDebateTranscriptRecord", "loadDebateTranscriptRecord"]
    - path: "src/app/paths.ts"
      provides: "Transcript run storage directory helpers"
      exports: ["getTranscriptRunsDir", "getTranscriptRunPath"]
  key_links:
    - from: "src/debate/transcript.ts"
      to: "src/app/paths.ts"
      via: "default run storage path resolution"
      pattern: "getTranscriptRunPath"
---

<objective>
Design and implement the transcript storage format.

Purpose: Establish a stable source-of-truth record for debate runs before persistence is threaded through execution.
Output: Typed transcript record helpers, predictable storage paths, and tests that can read the saved run artifact.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-transcript-persistence-and-inspection/03-CONTEXT.md
@.planning/phases/03-transcript-persistence-and-inspection/03-RESEARCH.md
@src/debate/run-debate.ts
@src/app/paths.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: add transcript schema and file path helpers</name>
  <files>src/app/paths.ts, src/debate/transcript.ts</files>
  <action>Create a dedicated transcript module that defines one structured JSON record per debate run, with top-level run metadata plus an explicit ordered stage array. Include stage status, actor role/model, prompt context, output content, timing, session IDs, and error fields. Add app path helpers for a default `runs/` directory and per-run JSON file paths.</action>
  <verify>
    <automated>npm run test -- --run tests/debate/run-debate.test.ts</automated>
    <sampling_rate>run after task completion</sampling_rate>
  </verify>
  <done>The codebase has a stable transcript record shape and a predictable storage path for saved runs.</done>
</task>

</tasks>

<verification>
- Transcript helpers create a record with explicit stage order and run metadata.
- Transcript helpers resolve a deterministic run file path.
</verification>

<success_criteria>
- One saved transcript record represents one debate run.
- The transcript schema stores both stage input context and stage output metadata.
- The storage path is stable enough for later CLI inspection.
</success_criteria>

<output>
After completion, create `.planning/phases/03-transcript-persistence-and-inspection/03-01-SUMMARY.md`
</output>
