---
phase: 03-transcript-persistence-and-inspection
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/debate/run-debate.ts
  - src/debate/state.ts
  - tests/debate/run-debate.test.ts
autonomous: true
requirements:
  - TRNS-01
must_haves:
  truths:
    - "Each stage transition is persisted into the transcript record while the debate is running, not only at the end."
    - "Successful stages save role, prompt context, output content, session metadata, and basic timing."
    - "Failed stages save their error details in the same transcript record so debugging uses one mental model."
  artifacts:
    - path: "src/debate/run-debate.ts"
      provides: "Incremental transcript persistence wired directly into stage execution"
      exports: ["runDebate"]
    - path: "src/debate/state.ts"
      provides: "Run-state status values that can represent persisted failure state"
      exports: ["StageStatus", "DebateRunState"]
  key_links:
    - from: "src/debate/run-debate.ts"
      to: "src/debate/transcript.ts"
      via: "save record on start, completion, and failure"
      pattern: "saveDebateTranscriptRecord"
---

<objective>
Persist stage artifacts as the debate progresses.

Purpose: Make the transcript durable and debuggable during execution instead of producing only an in-memory final state.
Output: Runner-integrated transcript writes for stage start, completion, placeholder stages, and failures.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-transcript-persistence-and-inspection/03-CONTEXT.md
@.planning/phases/03-transcript-persistence-and-inspection/03-RESEARCH.md
@.planning/phases/03-transcript-persistence-and-inspection/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>task 1: persist running, completed, and failed stage state into the transcript record</name>
  <files>src/debate/run-debate.ts, src/debate/state.ts, tests/debate/run-debate.test.ts</files>
  <action>Update the debate runner so it creates a transcript record at run start, marks each stage running before execution, saves stage input/output metadata after execution, and writes failure details back into the same record when a stage throws. Keep revise and judge stages visibly present with explicit placeholders instead of hiding them. Extend tests to verify saved transcript contents for successful and failed runs.</action>
  <verify>
    <automated>npm run test -- --run tests/debate/run-debate.test.ts</automated>
    <sampling_rate>run after task completion and before 03-03</sampling_rate>
  </verify>
  <done>The transcript record stays current while the debate runs and still contains useful debug data if execution fails mid-run.</done>
</task>

</tasks>

<verification>
- Successful runs write transcript files containing saved answer/critique artifacts.
- Failed runs leave a transcript record behind with the failed stage and error details.
</verification>

<success_criteria>
- Intermediate debate state is available on disk for debugging.
- Failed stages live in the same transcript model as successful stages.
- Stage timing and session metadata are persisted for implemented stages.
</success_criteria>

<output>
After completion, create `.planning/phases/03-transcript-persistence-and-inspection/03-02-SUMMARY.md`
</output>
