---
phase: 03-transcript-persistence-and-inspection
plan: "03"
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/cli/program.ts
  - src/cli/run-ask-command.ts
  - src/cli/run-inspect-command.ts
  - src/cli/render.ts
  - tests/cli/program.test.ts
  - tests/cli/run-inspect-command.test.ts
  - tests/debate/run-debate.test.ts
autonomous: true
requirements:
  - TRNS-02
must_haves:
  truths:
    - "A user can inspect a saved debate transcript after a run completes using a dedicated CLI command."
    - "The inspection output renders ordered stage sections with inline metadata and visible placeholders for unfinished later stages."
    - "Successful debate completion output tells the user where the transcript was saved and how to inspect it."
  artifacts:
    - path: "src/cli/run-inspect-command.ts"
      provides: "CLI inspection handler that loads a run record and renders the transcript report"
      exports: ["runInspectCommand"]
    - path: "src/cli/render.ts"
      provides: "Inspection report and transcript-aware completion rendering"
      exports: ["renderTranscriptInspection", "renderCompletion"]
    - path: "src/cli/program.ts"
      provides: "`inspect` command wiring"
      exports: ["buildProgram"]
  key_links:
    - from: "src/cli/run-inspect-command.ts"
      to: "src/debate/transcript.ts"
      via: "load saved record by run id"
      pattern: "loadDebateTranscriptRecord"
    - from: "src/cli/program.ts"
      to: "src/cli/run-inspect-command.ts"
      via: "inspect command action"
      pattern: "runInspectCommand"
---

<objective>
Add transcript inspection output for completed debates.

Purpose: Turn the saved transcript record into a readable user-facing report instead of leaving it as a hidden implementation detail.
Output: `debate inspect <run-id>`, transcript-aware completion messaging, and tests that verify the rendered report.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-transcript-persistence-and-inspection/03-CONTEXT.md
@.planning/phases/03-transcript-persistence-and-inspection/03-RESEARCH.md
@.planning/phases/03-transcript-persistence-and-inspection/03-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>task 1: add transcript inspection command and transcript-aware completion output</name>
  <files>src/cli/program.ts, src/cli/run-ask-command.ts, src/cli/run-inspect-command.ts, src/cli/render.ts, tests/cli/program.test.ts, tests/cli/run-inspect-command.test.ts, tests/debate/run-debate.test.ts</files>
  <action>Add an `inspect` command that loads a transcript by run id and prints a structured stage-by-stage report from the saved record. Update end-of-run completion output so successful debates clearly report the transcript id/path and the exact inspect command to run next. Keep the report readable, metadata-inline, and explicit about placeholder stages that do not yet have model output in this phase.</action>
  <verify>
    <automated>npm run test</automated>
    <sampling_rate>run after task completion and before phase verification</sampling_rate>
  </verify>
  <done>Users can inspect saved debates directly from the CLI and the end-of-run output points them to the transcript artifact.</done>
</task>

</tasks>

<verification>
- `debate inspect <run-id>` renders ordered stage sections from the saved record.
- End-of-run output includes transcript id/path and the inspect command.
- Placeholders are visible for later stages without saved model output.
</verification>

<success_criteria>
- User can inspect the transcript of a completed debate run.
- Inspection output is derived from the structured transcript record.
- The transcript report stays readable without falling back to raw JSON.
</success_criteria>

<output>
After completion, create `.planning/phases/03-transcript-persistence-and-inspection/03-03-SUMMARY.md`
</output>
