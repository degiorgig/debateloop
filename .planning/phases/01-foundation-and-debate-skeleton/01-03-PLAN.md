---
phase: 01-foundation-and-debate-skeleton
plan: "03"
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/cli/program.ts
  - src/debate/stages.ts
  - src/debate/state.ts
  - src/debate/run-debate.ts
  - src/cli/run-ask-command.ts
  - src/cli/render.ts
  - tests/debate/stages.test.ts
  - tests/debate/run-debate.test.ts
autonomous: true
requirements:
  - INPUT-01
  - ORCH-07
must_haves:
  truths:
    - "User can start a debate run with a question and immediately see the full fixed debate structure before execution begins."
    - "During execution, the CLI shows stage-by-stage progress with both the stage label and the acting role/model."
    - "The debate skeleton advances through one explicit fixed stage order without streaming a raw transcript by default."
  artifacts:
    - path: "src/debate/stages.ts"
      provides: "Single source of truth for the fixed ordered stage metadata"
      exports: ["DEBATE_STAGES"]
    - path: "src/debate/run-debate.ts"
      provides: "Skeleton orchestration loop over the fixed stage sequence"
      exports: ["runDebate"]
    - path: "src/cli/run-ask-command.ts"
      provides: "CLI handler that resolves config, prints active roles, and starts the skeleton run"
      exports: ["runAskCommand"]
    - path: "src/cli/program.ts"
      provides: "Commander ask command wired to the real run handler instead of the Phase 1 placeholder"
      exports: ["buildProgram"]
  key_links:
    - from: "src/cli/program.ts"
      to: "src/cli/run-ask-command.ts"
      via: "replace the placeholder ask action with the real handler"
      pattern: "runAskCommand"
    - from: "src/cli/run-ask-command.ts"
      to: "src/debate/run-debate.ts"
      via: "ask command handler"
      pattern: "runDebate"
    - from: "src/cli/run-ask-command.ts"
      to: "src/opencode/client.ts"
      via: "start OpenCode for the run and close it during cleanup"
      pattern: "startOpenCode|close"
    - from: "src/debate/stages.ts"
      to: "src/cli/render.ts"
      via: "display labels and acting-role metadata"
      pattern: "Answer A|Critique A|Final decision"
    - from: "src/debate/run-debate.ts"
      to: "src/debate/state.ts"
      via: "ordered stage state transitions"
      pattern: "currentStage|status"
---

<objective>
Define the fixed stage model and wire the first runnable debate skeleton.

Purpose: Make the Phase 1 app visibly execute the debate shape in a stable order before later phases add real answers, critiques, and judging behavior.
Output: Stage metadata, typed run state, a skeleton orchestrator, CLI progress rendering, real ask-command wiring, and deterministic tests for stage order and completion behavior.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-debate-skeleton/01-CONTEXT.md
@.planning/phases/01-foundation-and-debate-skeleton/01-RESEARCH.md
@.planning/phases/01-foundation-and-debate-skeleton/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>task 1: define the explicit fixed debate stage metadata and state types</name>
  <files>src/debate/stages.ts, src/debate/state.ts, tests/debate/stages.test.ts</files>
  <action>Create the fixed ordered debate stage list as a single source of truth with plain display labels and acting-role metadata. The visible labels must include `Answer A`, `Answer B`, `Critique A`, `Critique B`, and `Final decision`, while the full internal order also includes the two revise stages needed by later phases. Add typed run-state structures that can track the active question, role/model assignments, current stage, and per-stage status without introducing transcript persistence yet.</action>
  <verify>
    <automated>npm run test -- --run tests/debate/stages.test.ts</automated>
    <sampling_rate>run after task completion</sampling_rate>
  </verify>
  <done>The debate skeleton has one explicit, typed stage sequence that preserves the symmetric workflow and is guarded by automated tests.</done>
</task>

<task type="auto">
  <name>task 2: wire the runnable skeleton ask flow and progress rendering</name>
  <files>src/cli/program.ts, src/debate/run-debate.ts, src/cli/run-ask-command.ts, src/cli/render.ts</files>
  <action>Replace the Phase 1 placeholder `ask` action in `src/cli/program.ts` with the real `runAskCommand` handler. In that handler, resolve the active role config from `01-02`, start OpenCode via `src/opencode/client.ts`, print one compact summary line of the active Debater A / Debater B / Judge models, show the full debate structure before execution, and then run a placeholder orchestration loop across the fixed stage list. Ensure the OpenCode session is always closed after the run, including failure paths. During execution, render progress stage-by-stage with both the current stage label and the acting role/model. Keep transcript output hidden by default and offer it only after completion. For the completion view, make the renderer result-first in shape so later phases can lead with the winner, but in Phase 1 explicitly render a forward-compatible `decision pending` / transcript-offer state instead of fabricating a winner before judge logic exists. Also implement the locked one-time first-run help in the real completion path: after the first successful run following setup, render a brief rerun hint that includes one concise example command, then persist the app-owned first-run marker so later successful runs suppress the hint unless the user explicitly asks for help.</action>
  <verify>
    <automated>npm run test -- --run tests/debate/run-debate.test.ts</automated>
    <sampling_rate>run after task completion</sampling_rate>
  </verify>
  <done>A user can run a question through the skeleton flow, see the debate structure up front, and watch the fixed stages progress in order with role/model visibility.</done>
</task>

<task type="auto">
  <name>task 3: cover stage order and first-run completion behavior with tests</name>
  <files>tests/debate/run-debate.test.ts</files>
  <action>Add deterministic integration-style tests that invoke the ask flow with seeded role config and a mocked `src/opencode/client.ts` session so the suite never depends on interactive setup or a real OpenCode startup. Prove that the skeleton run walks the stages in the exact fixed order, uses the required plain labels, starts and closes the mocked OpenCode session once per run, and keeps default output result/progress focused instead of transcript streaming. Include coverage for the forward-compatible completion view by asserting Phase 1 ends in the result-first shell with no fabricated winner, plus the one-time first-run hint behavior appearing after the first successful run and being suppressed on later runs once the config marker is already persisted by task 2.</action>
  <verify>
    <automated>npm run test -- --run tests/debate/stages.test.ts tests/debate/run-debate.test.ts</automated>
    <sampling_rate>run after task completion and before phase handoff</sampling_rate>
  </verify>
  <done>The stage order, visibility rules, and first-run completion hint are protected by automated tests.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run tests/debate/stages.test.ts tests/debate/run-debate.test.ts` passes.
- The automated ask-flow tests seed config and mock OpenCode so they prove the runnable path without requiring interactive setup or a local OpenCode process.
- Default run output stays progress-first and transcript details remain opt-in after completion.
</verification>

<success_criteria>
- The debate stage order is explicit, typed, and visible.
- The app can start a new skeleton debate run from the CLI with saved role config, real ask-command wiring, and stage-by-stage progress.
- The completion renderer is shaped for the locked winner-first result screen while explicitly deferring winner output until judge behavior exists.
- Phase 1 delivers the structural app shell needed for real debate behavior in Phase 2.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-debate-skeleton/01-03-SUMMARY.md`
</output>
