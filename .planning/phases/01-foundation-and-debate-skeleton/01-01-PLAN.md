---
phase: 01-foundation-and-debate-skeleton
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - src/index.ts
  - src/cli/program.ts
  - tests/cli/program.test.ts
autonomous: true
requirements:
  - INPUT-01
must_haves:
  truths:
    - "User can invoke the tool with a direct quoted-question command instead of entering an app-first shell."
    - "The repository has a runnable Node 20+ TypeScript CLI entrypoint with build and test scripts."
    - "CLI help presents Debate as a practical terminal utility with readable command names and long-form flags."
  artifacts:
    - path: "package.json"
      provides: "Package metadata, scripts, and runtime dependencies for the TypeScript CLI"
      contains: "commander"
    - path: "src/index.ts"
      provides: "Executable entrypoint that boots the CLI"
      exports: ["main"]
    - path: "src/cli/program.ts"
      provides: "Commander program with the primary ask command"
      exports: ["buildProgram"]
  key_links:
    - from: "package.json"
      to: "src/index.ts"
      via: "dev/build/bin scripts"
      pattern: "src/index.ts|dist/index.js"
    - from: "src/index.ts"
      to: "src/cli/program.ts"
      via: "CLI bootstrap"
      pattern: "buildProgram"
---

<objective>
Create the runnable TypeScript CLI foundation for Debate.

Purpose: Establish the tool-first command surface, runtime scripts, and entrypoint needed for later OpenCode wiring.
Output: Package/runtime config, an executable CLI, and tests proving direct quoted-question input works.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-debate-skeleton/01-CONTEXT.md
@.planning/phases/01-foundation-and-debate-skeleton/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>task 1: bootstrap the Node ESM TypeScript toolchain</name>
  <files>package.json, tsconfig.json, vitest.config.ts</files>
  <action>Create a Node 20+ ESM package for Debate using the researched stack: `typescript`, `tsx`, `vitest`, `commander`, `zod`, `@inquirer/prompts`, and `@opencode-ai/sdk`. Add readable scripts for local execution, build, and test. Keep the package focused on a CLI app; do not add framework baggage, transcript persistence, or judge-specific dependencies in this phase.</action>
  <verify>
    <automated>npm install &amp;&amp; npm run build</automated>
    <sampling_rate>run after task completion</sampling_rate>
  </verify>
  <done>The repo installs cleanly, `npm run build` succeeds, and the package exposes the TypeScript CLI workflow needed for the next tasks.</done>
</task>

<task type="auto">
  <name>task 2: create the direct ask command entrypoint</name>
  <files>src/index.ts, src/cli/program.ts</files>
  <action>Implement a thin CLI bootstrap that exposes Debate through Commander with a primary `ask &lt;question&gt;` command and readable long-form flags such as `--debater-a`, `--debater-b`, and `--judge`. The command must accept a quoted question as the main input style and show Debate branding in help/output, while staying tool-first rather than shell-like. Use a minimal placeholder handler here that proves argument capture without pretending the full run flow already exists; the real orchestration lands in `01-03`.</action>
  <verify>
    <automated>npm run build &amp;&amp; node dist/index.js ask "Should tests come first?"</automated>
    <sampling_rate>run after task completion</sampling_rate>
  </verify>
  <done>Running the built CLI with `ask &lt;question&gt;` succeeds, the question is captured by the command handler, and help output reflects the plain readable command surface chosen for v1.</done>
</task>

<task type="auto">
  <name>task 3: lock in CLI parsing behavior with tests</name>
  <files>tests/cli/program.test.ts</files>
  <action>Add Vitest coverage for the command shape: direct `ask &lt;question&gt;` parsing, readable long-form role override flags, and help/usage text that emphasizes Debate as a practical command-line tool. Keep tests focused on command parsing and help text, not on OpenCode integration or debate execution yet.</action>
  <verify>
    <automated>npm run test -- --run tests/cli/program.test.ts</automated>
    <sampling_rate>run after task completion and before starting 01-02</sampling_rate>
  </verify>
  <done>The CLI contract is enforced by tests, including the direct quoted-question entrypoint required by INPUT-01.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds on a clean install.
- `npm run test -- --run tests/cli/program.test.ts` passes.
- `node dist/index.js ask "example question"` uses the direct command flow instead of launching an app-first shell.
</verification>

<success_criteria>
- The repository has a working TypeScript CLI foundation.
- Debate exposes a direct quoted-question `ask` command with readable flags.
- Command parsing behavior is covered by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-debate-skeleton/01-01-SUMMARY.md`
</output>
