---
phase: 01-foundation-and-debate-skeleton
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/paths.ts
  - src/app/config.ts
  - src/opencode/client.ts
  - src/opencode/models.ts
  - src/cli/setup.ts
  - src/cli/resolve-run-config.ts
  - tests/app/config.test.ts
  - tests/opencode/models.test.ts
  - tests/cli/resolve-run-config.test.ts
autonomous: true
requirements:
  - INPUT-02
  - INPUT-03
must_haves:
  truths:
    - "User can load saved defaults for Debater A, Debater B, and Judge or be guided through setup when config is missing or stale."
    - "Debater A and Debater B cannot resolve to the same model in v1."
    - "OpenCode startup and model availability failures stop immediately with friendly guidance that names the affected role and model."
  artifacts:
    - path: "src/app/config.ts"
      provides: "Debate-owned role defaults schema, validation, and persistence"
      exports: ["DebateConfigSchema"]
    - path: "src/opencode/client.ts"
      provides: "OpenCode startup and connectivity wrapper around the SDK"
      exports: ["startOpenCode"]
    - path: "src/cli/resolve-run-config.ts"
      provides: "Runtime config resolution combining saved defaults, overrides, and validation"
      exports: ["resolveRunConfig"]
  key_links:
    - from: "src/app/config.ts"
      to: "src/cli/resolve-run-config.ts"
      via: "load/validate saved defaults"
      pattern: "load.*config|safeParse"
    - from: "src/opencode/models.ts"
      to: "src/cli/setup.ts"
      via: "available model choices for interactive setup"
      pattern: "providers|choices"
    - from: "src/cli/resolve-run-config.ts"
      to: "src/opencode/models.ts"
      via: "stale model availability checks"
      pattern: "assert.*Available|validate"
---

<objective>
Wire Debate's app-owned model-role configuration and the initial OpenCode integration.

Purpose: Let the CLI resolve Debater A, Debater B, and Judge models from saved defaults or guided setup before the debate skeleton runs.
Output: Persisted role config, OpenCode client helpers, runtime model validation, and setup/override resolution that the runnable ask flow can consume in 01-03.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-debate-skeleton/01-CONTEXT.md
@.planning/phases/01-foundation-and-debate-skeleton/01-RESEARCH.md
@.planning/phases/01-foundation-and-debate-skeleton/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>task 1: implement Debate-owned config persistence and validation</name>
  <files>src/app/paths.ts, src/app/config.ts, tests/app/config.test.ts</files>
  <action>Create a small app config path helper and a Zod-backed Debate config module that stores only Debate-owned state: Debater A, Debater B, Judge, and the first-run hint marker. Validate the distinct-debaters rule at the load boundary and surface friendly parse errors. Do not write role defaults into `opencode.json` or any other OpenCode config file.</action>
  <verify>
    <automated>npm run test -- --run tests/app/config.test.ts</automated>
    <sampling_rate>run after task completion</sampling_rate>
  </verify>
  <done>Debate can safely load and save its own config, invalid configs are rejected clearly, and the `debaterA !== debaterB` rule is enforced.</done>
</task>

<task type="auto">
  <name>task 2: add OpenCode startup and model availability helpers</name>
  <files>src/opencode/client.ts, src/opencode/models.ts, tests/opencode/models.test.ts</files>
  <action>Wrap `@opencode-ai/sdk` startup around `α1()` in a focused helper that can start OpenCode, expose the typed client, and close the server handle cleanly. Shape the helper as a small reusable boundary for later consumption by the real `ask` flow in `01-03`: one entrypoint to start the session before execution and one guaranteed close path for cleanup after success or failure. Add model discovery and validation helpers based on `client.config.providers()` / `client.config.get()` so Debate can detect missing or stale model IDs before a run begins. Fail fast when OpenCode is unavailable and include clear, friendly fix steps instead of raw SDK stack traces.</action>
  <verify>
    <automated>npm run test -- --run tests/opencode/models.test.ts</automated>
    <sampling_rate>run after task completion</sampling_rate>
  </verify>
  <done>Debate can start OpenCode programmatically, inspect available models, and produce role/model-specific startup errors before orchestration begins.</done>
</task>

<task type="auto">
  <name>task 3: resolve saved defaults, per-run overrides, and guided setup</name>
  <files>src/cli/setup.ts, src/cli/resolve-run-config.ts, tests/cli/resolve-run-config.test.ts</files>
  <action>Implement the first-run and stale-config path: when saved defaults are missing, incomplete, or invalid against current OpenCode models, prompt the user through interactive role selection with `@inquirer/prompts` and persist the repaired defaults. Support `--debater-a`, `--debater-b`, and `--judge` as per-run overrides that affect only the current run. Keep this plan focused on producing one validated runtime role config object plus any setup persistence needed by later execution code; defer real ask-command wiring, OpenCode session lifecycle usage, and post-run rendering to `01-03`. Keep the UX command-first: prompts are only for missing/incomplete setup, not a general interactive shell.</action>
  <verify>
    <automated>npm run test -- --run tests/cli/resolve-run-config.test.ts</automated>
    <sampling_rate>run after task completion and before starting 01-03</sampling_rate>
  </verify>
  <done>The CLI can produce one resolved set of active roles/models for a run, prompting only when needed and respecting saved defaults plus non-persistent overrides, without yet owning the execution loop.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run tests/app/config.test.ts tests/opencode/models.test.ts tests/cli/resolve-run-config.test.ts` passes.
- Missing config triggers guided role setup instead of silent guessing.
- Unavailable OpenCode or unavailable models fail before the run starts with clear guidance naming the affected role/model.
</verification>

<success_criteria>
- Debate owns its own persisted role defaults separate from OpenCode config.
- The app can start OpenCode and validate model availability through the SDK.
- The CLI resolves saved defaults and one-run overrides into a validated active role set that 01-03 can execute.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-debate-skeleton/01-02-SUMMARY.md`
</output>
