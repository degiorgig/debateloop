---
phase: 05-reliability-and-v1-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/config.ts
  - src/debate/run-debate.ts
  - src/debate/state.ts
  - src/cli/run-ask-command.ts
  - src/cli/render.ts
  - tests/app/config.test.ts
  - tests/debate/run-debate.test.ts
  - tests/cli/render.test.ts
autonomous: true
requirements:
  - RELY-01
  - RELY-02
must_haves:
  truths:
    - "Users can tell which exact debate stage failed, which model was running, and why it failed."
    - "Recoverable provider-style failures retry conservatively instead of failing immediately."
    - "Retry activity is visible to the user instead of silent."
  artifacts:
    - path: "src/debate/run-debate.ts"
      provides: "Bounded retry and timeout-aware stage execution with stage-aware execution errors"
      exports: ["runDebate", "DebateStageExecutionError"]
    - path: "src/app/config.ts"
      provides: "Persisted reliability defaults for retries and timeouts"
      exports: ["DebateConfigSchema", "parseDebateConfig"]
    - path: "src/cli/render.ts"
      provides: "Retry and failure rendering for debate ask output"
      exports: ["renderStageRetry", "renderFailure"]
  key_links:
    - from: "src/cli/run-ask-command.ts"
      to: "src/debate/run-debate.ts"
      via: "retry callback and stage-aware error translation"
      pattern: "runDebate"
    - from: "src/debate/run-debate.ts"
      to: "src/app/config.ts"
      via: "reliability settings"
      pattern: "reliability"
---

<objective>
Add retries, timeout configuration, and stage-specific error surfaces.

Purpose: Make the debate engine resilient to transient provider problems while still failing clearly when recovery is not possible.
Output: Conservative reliability config, bounded retry/timeout stage execution, visible retry logging, and stage-aware failure output.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reliability-and-v1-polish/05-CONTEXT.md
@.planning/phases/05-reliability-and-v1-polish/05-RESEARCH.md
@src/app/config.ts
@src/debate/run-debate.ts
@src/cli/run-ask-command.ts
@src/cli/render.ts
@tests/debate/run-debate.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: add conservative reliability settings and bounded stage retries</name>
  <files>src/app/config.ts, src/debate/run-debate.ts, src/debate/state.ts, tests/app/config.test.ts, tests/debate/run-debate.test.ts</files>
  <action>Add a small persisted `reliability` config block with conservative defaults for max attempts, timeout, and retry backoff. Update stage execution so model-backed stages can retry only recoverable provider or timeout failures, keep retry counts bounded, and preserve current stage ordering and state semantics. Failures that are not recoverable should still stop immediately. Keep the engine API compact: a retry callback and a structured stage-aware error type are enough for downstream CLI use.</action>
  <verify>
    <automated>npm run test -- --run tests/app/config.test.ts tests/debate/run-debate.test.ts</automated>
    <sampling_rate>run after task completion</sampling_rate>
  </verify>
  <done>The engine has conservative retry/timeout behavior and tests prove retryable vs non-retryable stage handling.</done>
</task>

<task type="auto">
  <name>task 2: expose retry activity and stage-specific failures through the ask CLI</name>
  <files>src/cli/run-ask-command.ts, src/cli/render.ts, tests/cli/render.test.ts, tests/debate/run-debate.test.ts</files>
  <action>Wire the new retry callback into `debate ask` so retry activity is visible in terminal output. Add a dedicated failure render path that leads with the failed stage, model, and reason, then points users to the saved transcript artifact and `debate inspect` before retrying blindly. Keep the success path unchanged apart from the new reliability-aware wiring.</action>
  <verify>
    <automated>npm run test -- --run tests/cli/render.test.ts tests/debate/run-debate.test.ts && npm run build</automated>
    <sampling_rate>run after task completion and before 05-02</sampling_rate>
  </verify>
  <done>`debate ask` now makes retries visible and surfaces unrecovered failures as stage-aware actionable output.</done>
</task>

</tasks>

<verification>
- Retryable stage failures trigger a bounded retry instead of immediate failure.
- The terminal shows which stage is being retried and why.
- Final unrecovered failures name the stage, actor model, and next inspection step.
</verification>

<success_criteria>
- Users can tell which debate stage failed and why.
- Recoverable failures can be retried conservatively without changing debate stage order.
- Retry visibility is present in CLI output.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reliability-and-v1-polish/05-01-SUMMARY.md`
</output>
