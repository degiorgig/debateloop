---
phase: 05-reliability-and-v1-polish
plan: "02"
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/debate/transcript.ts
  - src/debate/run-debate.ts
  - src/cli/render.ts
  - src/cli/run-inspect-command.ts
  - tests/cli/run-inspect-command.test.ts
  - tests/debate/run-debate.test.ts
  - tests/cli/render.test.ts
autonomous: true
requirements:
  - RELY-03
must_haves:
  truths:
    - "Partial failures preserve usable transcript data instead of discarding earlier completed stages."
    - "Failed transcripts are clearly labeled as preserved partial runs during inspection."
    - "Transcript writes are durable enough that failed runs do not leave obvious half-written artifacts."
  artifacts:
    - path: "src/debate/transcript.ts"
      provides: "Atomic transcript persistence with explicit failed-stage metadata"
      exports: ["saveDebateTranscriptRecord", "loadDebateTranscriptRecord"]
    - path: "src/cli/render.ts"
      provides: "Partial-run transcript inspection summary"
      exports: ["renderTranscriptInspection"]
    - path: "tests/cli/run-inspect-command.test.ts"
      provides: "Coverage for failed transcript labeling and attempt visibility"
  key_links:
    - from: "src/debate/run-debate.ts"
      to: "src/debate/transcript.ts"
      via: "failed stage persistence and transcript durability"
      pattern: "saveDebateTranscriptRecord"
    - from: "src/cli/run-inspect-command.ts"
      to: "src/cli/render.ts"
      via: "partial transcript display"
      pattern: "renderTranscriptInspection"
---

<objective>
Protect transcript integrity during partial failures and aborts.

Purpose: Turn partial failed runs into reliable debugging artifacts rather than fragile leftovers.
Output: More durable transcript writes, explicit failed-stage metadata, and clearer inspect output for partial runs.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reliability-and-v1-polish/05-CONTEXT.md
@.planning/phases/05-reliability-and-v1-polish/05-RESEARCH.md
@.planning/phases/05-reliability-and-v1-polish/05-01-PLAN.md
@src/debate/transcript.ts
@src/debate/run-debate.ts
@src/cli/render.ts
@src/cli/run-inspect-command.ts
@tests/cli/run-inspect-command.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: harden transcript persistence for failed runs</name>
  <files>src/debate/transcript.ts, src/debate/run-debate.ts, tests/debate/run-debate.test.ts</files>
  <action>Make transcript persistence more durable for v1 by writing through a temp file and renaming into place, and extend transcript metadata so failed runs record the failed stage explicitly plus useful attempt metadata. Keep the existing incremental persistence approach intact so earlier completed stages remain inspectable after a failure.</action>
  <verify>
    <automated>npm run test -- --run tests/debate/run-debate.test.ts</automated>
    <sampling_rate>run after task completion</sampling_rate>
  </verify>
  <done>Failed runs preserve explicit failed-stage metadata and transcript writes are more robust against partial overwrite risk.</done>
</task>

<task type="auto">
  <name>task 2: mark failed inspect output as preserved partial transcript data</name>
  <files>src/cli/render.ts, src/cli/run-inspect-command.ts, tests/cli/run-inspect-command.test.ts, tests/cli/render.test.ts</files>
  <action>Update transcript inspection rendering so failed runs are explicitly described as partial preserved artifacts, summarize completed stage count and failed stage when available, and show retry attempt count where it helps debugging. Keep completed-run inspection behavior readable and unchanged aside from the additional metadata support.</action>
  <verify>
    <automated>npm run test -- --run tests/cli/run-inspect-command.test.ts tests/cli/render.test.ts && npm run build</automated>
    <sampling_rate>run after task completion and before 05-03</sampling_rate>
  </verify>
  <done>`debate inspect` clearly communicates that a failed run is still a useful partial artifact for debugging.</done>
</task>

</tasks>

<verification>
- Failed runs keep earlier completed stage content in the saved transcript.
- Transcript metadata records which stage failed.
- Inspect output explicitly marks failed runs as partial preserved transcripts.
</verification>

<success_criteria>
- Partial failures preserve usable transcript data for debugging.
- Partial-run artifacts are clearly marked and easier to interpret.
- Transcript persistence is less likely to leave corrupted visible output files.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reliability-and-v1-polish/05-02-SUMMARY.md`
</output>
