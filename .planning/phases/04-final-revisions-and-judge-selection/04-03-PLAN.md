---
phase: 04-final-revisions-and-judge-selection
plan: "03"
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/cli/render.ts
  - src/cli/run-ask-command.ts
  - tests/cli/render.test.ts
  - tests/debate/run-debate.test.ts
autonomous: true
requirements:
  - TRNS-03
must_haves:
  truths:
    - "The final app output leads with the winning debater and exact winning model name."
    - "The default final result shows the winning final revised answer and a short rationale."
    - "The losing revised answer is suppressed from the default final output."
  artifacts:
    - path: "src/cli/render.ts"
      provides: "Winner-first final result rendering"
      exports: ["renderCompletion"]
    - path: "src/cli/run-ask-command.ts"
      provides: "Ask flow wiring from validated judge result to final terminal output"
      exports: ["runAskCommand"]
    - path: "tests/cli/render.test.ts"
      provides: "Focused assertions for winner-first verdict formatting and losing-answer suppression"
  key_links:
    - from: "src/cli/run-ask-command.ts"
      to: "src/debate/run-debate.ts"
      via: "validated winner and rationale passed into final rendering"
      pattern: "runDebate"
    - from: "src/cli/render.ts"
      to: "revise_a|revise_b"
      via: "winner maps to the winning revised answer only"
      pattern: "Winning Answer:"
---

<objective>
Render the final verdict and winning answer from the validated judge result.

Purpose: Turn the structured judge decision into a decisive user-facing result instead of leaving the CLI at `Decision: pending`.
Output: Winner-first completion output, ask-command wiring for validated verdict data, and tests proving the losing answer is hidden by default.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-final-revisions-and-judge-selection/04-CONTEXT.md
@.planning/phases/04-final-revisions-and-judge-selection/04-RESEARCH.md
@.planning/phases/03-transcript-persistence-and-inspection/03-03-SUMMARY.md
@.planning/phases/04-final-revisions-and-judge-selection/04-02-PLAN.md
@src/cli/render.ts
@src/cli/run-ask-command.ts
@tests/debate/run-debate.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: add winner-first final result rendering</name>
  <files>src/cli/render.ts, tests/cli/render.test.ts</files>
  <action>Replace the pending-decision render path with a winner-first final result format that matches the locked user decisions: lead with the winner, identify that winner as both debate role and exact model name, show the winning final revised answer next, keep the rationale short by default, and do not show the losing revised answer in the default output. Keep the transcript id/path plus `debate inspect` pointer visible so the full run remains inspectable separately.</action>
  <verify>
    <automated>npm run test -- --run tests/cli/render.test.ts && npm run build</automated>
    <sampling_rate>run after task completion</sampling_rate>
  </verify>
  <done>The renderer can produce a decisive winner-first result that hides the losing answer by default.</done>
</task>

<task type="auto">
  <name>task 2: wire the ask command to show the validated final verdict</name>
  <files>src/cli/run-ask-command.ts, tests/debate/run-debate.test.ts</files>
  <action>Update the ask flow so it consumes the validated judge result returned from `runDebate`, resolves the winning model from the winning role, selects the corresponding revised-answer stage content, and logs the final winner-first output instead of `Decision: pending`. Expand CLI-flow coverage in `tests/debate/run-debate.test.ts` so a completed run proves the winner, exact model name, short rationale, winning revised answer, and transcript inspect command are all present while the losing revised answer is absent.</action>
  <verify>
    <automated>npm run test -- --run tests/cli/render.test.ts tests/debate/run-debate.test.ts && npm run build</automated>
    <sampling_rate>run after task completion and before phase verification</sampling_rate>
  </verify>
  <done>Running `debate ask` ends with a validated winner-first verdict instead of a pending decision placeholder.</done>
</task>

</tasks>

<verification>
- The final CLI output names the winner as both debater role and exact model id.
- The winning revised answer is shown directly in the default result.
- The losing revised answer does not appear in the default result.
- Transcript inspection remains reachable from the completion output.
</verification>

<success_criteria>
- Final app output shows the winning model, rationale, and winning answer clearly.
- Default output feels deterministic and decisive instead of provisional.
- The losing revised answer is only available through transcript inspection, not the default result.
</success_criteria>

<output>
After completion, create `.planning/phases/04-final-revisions-and-judge-selection/04-03-SUMMARY.md`
</output>
