---
phase: 04-final-revisions-and-judge-selection
plan: "02"
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/debate/prompts.ts
  - src/debate/judge.ts
  - src/debate/run-debate.ts
  - tests/debate/judge.test.ts
  - tests/debate/run-debate.test.ts
autonomous: true
requirements:
  - JUDGE-01
  - JUDGE-02
  - JUDGE-03
must_haves:
  truths:
    - "The judge compares the two final revised answers and always selects exactly one winner."
    - "The accepted judge verdict includes a short rationale explaining why the winner was selected."
    - "Malformed or ambiguous judge output fails clearly before the app accepts the final decision."
  artifacts:
    - path: "src/debate/judge.ts"
      provides: "Structured judge verdict schema, parser, and validation errors"
      exports: ["JudgeVerdictSchema", "parseJudgeVerdict"]
    - path: "src/debate/prompts.ts"
      provides: "Structured judge prompt builder for revised-answer comparison"
      exports: ["buildJudgePrompt"]
    - path: "src/debate/run-debate.ts"
      provides: "Real final_decision execution with validated winner extraction"
      exports: ["runDebate"]
  key_links:
    - from: "src/debate/run-debate.ts"
      to: "src/debate/prompts.ts"
      via: "judge-stage prompt generation"
      pattern: "buildJudgePrompt"
    - from: "src/debate/run-debate.ts"
      to: "src/debate/judge.ts"
      via: "post-response schema validation"
      pattern: "parseJudgeVerdict"
    - from: "src/debate/run-debate.ts"
      to: "revise_a|revise_b"
      via: "required revised answers before final decision runs"
      pattern: "getRequiredStageContent"
---

<objective>
Add structured judge prompt, schema validation, and winner extraction.

Purpose: Make the final decision deterministic for the user instead of relying on unstructured model prose.
Output: A JSON-only judge contract, validated judge execution for `final_decision`, and tests covering success plus malformed-output failure paths.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-final-revisions-and-judge-selection/04-CONTEXT.md
@.planning/phases/04-final-revisions-and-judge-selection/04-RESEARCH.md
@.planning/phases/04-final-revisions-and-judge-selection/04-01-PLAN.md
@src/app/config.ts
@src/debate/prompts.ts
@src/debate/run-debate.ts
@tests/debate/run-debate.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: define the judge verdict contract and validate it with zod</name>
  <files>src/debate/prompts.ts, src/debate/judge.ts, tests/debate/judge.test.ts</files>
  <action>Create a new `src/debate/judge.ts` module that defines the structured verdict contract with Zod and parses raw model output through `JSON.parse` plus `safeParse`. The accepted verdict must always include exactly one winner (`debaterA` or `debaterB`) and a short rationale; do not allow a no-winner state. Update `buildJudgePrompt` in `src/debate/prompts.ts` to compare the two revised answers and to reflect the locked user decisions exactly: prioritize usefulness plus correctness, treat clarity/readability as major factors, penalize revised answers that ignore strong critique, stay balanced between safer and more decisive answers, and return only valid JSON. Add focused parser tests for valid verdicts, invalid JSON, wrong enum values, and malformed rationale.</action>
  <verify>
    <automated>npm run test -- --run tests/debate/judge.test.ts && npm run build</automated>
    <sampling_rate>run after task completion</sampling_rate>
  </verify>
  <done>The codebase has a reusable validated judge verdict parser and an explicit JSON-only prompt contract.</done>
</task>

<task type="auto">
  <name>task 2: execute final_decision with validated winner extraction</name>
  <files>src/debate/run-debate.ts, tests/debate/run-debate.test.ts</files>
  <action>Replace the `final_decision` placeholder with a real judge stage. Require `revise_a` and `revise_b` content before execution, run the judge in a fresh OpenCode session, store the raw judge response in the transcript/stage output, and validate it immediately with `parseJudgeVerdict` before the debate is treated as successful. Surface the validated winner and rationale in the `runDebate` completion payload so the CLI can render them without reparsing transcript text. If the judge output is malformed or unclear, fail the stage clearly instead of guessing or silently masking the problem.</action>
  <verify>
    <automated>npm run test -- --run tests/debate/judge.test.ts tests/debate/run-debate.test.ts && npm run build</automated>
    <sampling_rate>run after task completion and before 04-03</sampling_rate>
  </verify>
  <done>`final_decision` runs against the revised answers, the winner/rationale are extracted from validated structured output, and invalid judge output fails visibly.</done>
</task>

</tasks>

<verification>
- The judge prompt asks for JSON only and compares the two revised answers, not the opening answers.
- Valid judge output produces a winner plus rationale that the app can consume deterministically.
- Invalid judge output throws a clear validation error and marks the run as failed.
</verification>

<success_criteria>
- Judge can compare the two final revised answers and select a winner.
- Judge returns a rationale explaining why the winning answer was selected.
- Judge output is validated structurally before the app accepts the final result.
</success_criteria>

<output>
After completion, create `.planning/phases/04-final-revisions-and-judge-selection/04-02-SUMMARY.md`
</output>
