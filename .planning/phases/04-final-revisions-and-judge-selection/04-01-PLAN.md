---
phase: 04-final-revisions-and-judge-selection
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/debate/prompts.ts
  - src/debate/run-debate.ts
  - tests/debate/run-debate.test.ts
autonomous: true
requirements:
  - ORCH-05
  - ORCH-06
must_haves:
  truths:
    - "Both debaters can generate final revised answers after the critique round."
    - "Each revision directly addresses the opponent's critique while staying readable and self-contained."
    - "Revised answers remain distinct in voice and structure instead of collapsing into near-duplicates."
  artifacts:
    - path: "src/debate/prompts.ts"
      provides: "Balanced revision prompt builder with answer-plus-critique context"
      exports: ["buildRevisionPrompt"]
    - path: "src/debate/run-debate.ts"
      provides: "Real revise_a and revise_b execution with stored stage outputs"
      exports: ["runDebate"]
    - path: "tests/debate/run-debate.test.ts"
      provides: "Coverage for revision prompt context and stored revision results"
  key_links:
    - from: "src/debate/run-debate.ts"
      to: "src/debate/prompts.ts"
      via: "revision-stage prompt generation"
      pattern: "buildRevisionPrompt"
    - from: "src/debate/run-debate.ts"
      to: "answer_a|answer_b|critique_a|critique_b"
      via: "dependent stage content lookup before revision execution"
      pattern: "getRequiredStageContent"
---

<objective>
Implement final revision stages for Debater A and Debater B.

Purpose: Replace the placeholder revision stages with real symmetric model calls so the critique round materially improves both final answers.
Output: Revision prompt builders, executed `revise_a`/`revise_b` stages, and tests proving each revision sees the right context.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-final-revisions-and-judge-selection/04-CONTEXT.md
@.planning/phases/04-final-revisions-and-judge-selection/04-RESEARCH.md
@.planning/phases/02-independent-answers-and-critiques/02-02-SUMMARY.md
@.planning/phases/03-transcript-persistence-and-inspection/03-02-SUMMARY.md
@src/debate/prompts.ts
@src/debate/run-debate.ts
@tests/debate/run-debate.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: add balanced revision prompt builders with critique-aware guidance</name>
  <files>src/debate/prompts.ts</files>
  <action>Add `buildRevisionPrompt` beside the existing answer and critique builders. The prompt must honor the locked user decisions exactly: require a balanced rewrite rather than a tiny patch or a total from-scratch replacement, make addressing the opponent critique the main purpose, include some acknowledgment of critique without losing readability, and preserve each debater's distinct voice and structure. Reuse the existing shared framing and debater role-brief pattern so the revision round stays symmetric with the earlier stages.</action>
  <verify>
    <automated>npm run test -- --run tests/debate/run-debate.test.ts</automated>
    <sampling_rate>run after task completion</sampling_rate>
  </verify>
  <done>`src/debate/prompts.ts` can build a revision prompt for either debater using question + opening answer + opponent critique context.</done>
</task>

<task type="auto">
  <name>task 2: execute revise_a and revise_b through fresh symmetric sessions</name>
  <files>src/debate/run-debate.ts, tests/debate/run-debate.test.ts</files>
  <action>Replace the `revise_a` and `revise_b` placeholders with real model-backed execution inside `maybeRunStage`. Like the opening answers, create a fresh OpenCode session for each revision stage. For `revise_a`, require `answer_a` plus `critique_b`; for `revise_b`, require `answer_b` plus `critique_a`. Persist the generated revision content and metadata into stage state/transcript instead of placeholder text. Expand `tests/debate/run-debate.test.ts` so it proves the revision prompts include the actor's opening answer and opponent critique, and that the completed run stores real revised answers for both debaters.</action>
  <verify>
    <automated>npm run test -- --run tests/debate/run-debate.test.ts && npm run build</automated>
    <sampling_rate>run after task completion and before 04-02</sampling_rate>
  </verify>
  <done>`revise_a` and `revise_b` run as real stages, consume the correct prior outputs, and save non-placeholder final revised answers.</done>
</task>

</tasks>

<verification>
- A completed debate run contains real `revise_a` and `revise_b` outputs instead of placeholder content.
- Revision prompts include the actor's own opening answer and the opponent's critique of that answer.
- Revision prompts explicitly preserve distinct debater voice while requiring critique-driven improvement.
</verification>

<success_criteria>
- Debater A can generate a final revised answer after the critique round.
- Debater B can generate a final revised answer after the critique round.
- Stored transcript/state data for both revision stages contains real model output and metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/04-final-revisions-and-judge-selection/04-01-SUMMARY.md`
</output>
