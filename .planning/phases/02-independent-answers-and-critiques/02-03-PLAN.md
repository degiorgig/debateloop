---
phase: 02-independent-answers-and-critiques
plan: "03"
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - tests/debate/run-debate.test.ts
  - tests/debate/stages.test.ts
autonomous: true
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-03
  - ORCH-04
must_haves:
  truths:
    - "The stage order and independence boundaries are enforced by automated tests, not just by convention."
    - "Tests fail if Debater B's opening-answer prompt includes Debater A's answer."
    - "Tests fail if critique stages stop receiving both opening answers."
  artifacts:
    - path: "tests/debate/run-debate.test.ts"
      provides: "Integration-style coverage for stage execution, prompt boundaries, and independence messaging"
    - path: "tests/debate/stages.test.ts"
      provides: "Guardrail for the fixed visible stage order"
  key_links:
    - from: "tests/debate/run-debate.test.ts"
      to: "src/debate/run-debate.ts"
      via: "mocked SDK client and prompt payload assertions"
      pattern: "session\.create|session\.prompt"
---

<objective>
Lock the Phase 2 behavior down with tests around stage order and independence.

Purpose: Make the core answer/critique symmetry durable so later transcript and judge phases can build on it without re-opening the independence contract.
Output: Deterministic tests for prompt boundaries, critique context shape, visible messaging, and the unchanged fixed stage order.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-independent-answers-and-critiques/02-CONTEXT.md
@.planning/phases/02-independent-answers-and-critiques/02-RESEARCH.md
@.planning/phases/02-independent-answers-and-critiques/02-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>task 1: add boundary tests for opening answers versus critiques</name>
  <files>tests/debate/run-debate.test.ts</files>
  <action>Expand the mocked OpenCode integration tests so they capture every `session.create` and `session.prompt` call. Assert that `answer_a` and `answer_b` prompts contain the question and role framing only, with Debater B's prompt excluding Debater A's generated answer. Assert that `critique_a` and `critique_b` prompts include both opening answers in the correct actor/opponent arrangement. Keep the suite deterministic by mocking all SDK responses with simple text parts.</action>
  <verify>
    <automated>npm run test -- --run tests/debate/run-debate.test.ts</automated>
    <sampling_rate>run after task completion</sampling_rate>
  </verify>
  <done>The tests make the independence contract executable: they fail on answer leakage and fail when critique context is incomplete.</done>
</task>

<task type="auto">
  <name>task 2: keep the visible stage contract and phase behavior green end-to-end</name>
  <files>tests/debate/stages.test.ts, tests/debate/run-debate.test.ts</files>
  <action>Retain the existing fixed stage-order assertions and add end-to-end expectations for the visible independence note plus the completed answer/critique stages in run state. Finish with the focused debate test run and the full suite to confirm Phase 2 fits the current repo without regressions.</action>
  <verify>
    <automated>npm run test</automated>
    <sampling_rate>run after task completion and before phase handoff</sampling_rate>
  </verify>
  <done>Phase 2 behavior is protected by targeted tests and the repo-wide suite remains green.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run tests/debate/stages.test.ts tests/debate/run-debate.test.ts` passes.
- `npm run test` passes.
- The answer-stage independence contract and critique-stage shared-context contract are both asserted directly in tests.
</verification>

<success_criteria>
- Tests prove Debater B's opening answer remains independent.
- Tests prove both critique stages receive the two opening answers.
- Phase 2 has durable regression coverage for its core behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-independent-answers-and-critiques/02-03-SUMMARY.md`
</output>
