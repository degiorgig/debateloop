---
phase: 02-independent-answers-and-critiques
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/debate/prompts.ts
  - src/debate/run-debate.ts
  - src/cli/render.ts
  - tests/debate/run-debate.test.ts
autonomous: true
requirements:
  - ORCH-03
  - ORCH-04
must_haves:
  truths:
    - "Debater A can critique Debater B's opening answer in the right stage order."
    - "Debater B can critique Debater A's opening answer symmetrically."
    - "The user can see that the two opening answers were generated independently before critique starts."
  artifacts:
    - path: "src/debate/prompts.ts"
      provides: "Critique prompt builders that include both opening answers and actor-specific framing"
      exports: ["buildCritiquePrompt"]
    - path: "src/debate/run-debate.ts"
      provides: "SDK-backed execution for critique_a and critique_b"
      exports: ["runDebate"]
    - path: "src/cli/render.ts"
      provides: "Visible independence note for the opening-answer portion of the run"
      exports: ["renderIndependenceNote"]
  key_links:
    - from: "src/debate/run-debate.ts"
      to: "src/debate/prompts.ts"
      via: "critique stage prompt assembly"
      pattern: "buildCritiquePrompt"
    - from: "src/cli/render.ts"
      to: "src/cli/run-ask-command.ts"
      via: "opening-answer independence messaging"
      pattern: "independent"
---

<objective>
Implement the cross-critique stages and make the independence promise visible.

Purpose: Complete the core Phase 2 debate loop so each debater can read both opening answers and directly critique the other side without losing symmetry.
Output: Critique prompt builders, critique-stage execution, visible independence messaging, and tests that lock the answer-to-critique boundary down.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-independent-answers-and-critiques/02-CONTEXT.md
@.planning/phases/02-independent-answers-and-critiques/02-RESEARCH.md
@.planning/phases/02-independent-answers-and-critiques/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>task 1: add symmetric critique prompt builders with both opening answers</name>
  <files>src/debate/prompts.ts</files>
  <action>Extend the prompt module with a critique builder that accepts the question, actor role, actor opening answer, and opponent opening answer. Keep the framing symmetric, but preserve distinct role briefs so critique voice stays differentiated. Make the critique prompts comparison-oriented: each debater should evaluate the opponent answer against the actor's own answer instead of writing a generic follow-up.</action>
  <verify>
    <automated>npm run test -- --run tests/debate/run-debate.test.ts</automated>
    <sampling_rate>run after task completion</sampling_rate>
  </verify>
  <done>The code can build Critique A and Critique B prompts that include the exact two opening answers and preserve debater-specific voice.</done>
</task>

<task type="auto">
  <name>task 2: execute critique_a and critique_b and expose independence messaging</name>
  <files>src/debate/run-debate.ts, src/cli/render.ts, tests/debate/run-debate.test.ts</files>
  <action>Teach `runDebate` to execute `critique_a` and `critique_b` with the prompt builder from task 1, store the critique results in stage state, and leave revise/judge stages in placeholder mode. Add a concise renderer note that explicitly tells the user the opening answers were produced independently before the cross-critique exchange starts. Update the debate tests so they assert critique stages receive both answers while the opening-answer stages still do not.</action>
  <verify>
    <automated>npm run test -- --run tests/debate/run-debate.test.ts</automated>
    <sampling_rate>run after task completion and before 02-03</sampling_rate>
  </verify>
  <done>Each debater can critique the other opening answer in the correct order, the independence note is visible in the CLI output, and tests distinguish answer-stage isolation from critique-stage shared context.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run tests/debate/run-debate.test.ts` passes.
- `critique_a` and `critique_b` prompts both include the actor answer and opponent answer.
- The CLI output explicitly states that opening answers were generated independently before critique begins.
</verification>

<success_criteria>
- Debater A critiques Debater B's opening answer.
- Debater B critiques Debater A's opening answer.
- The core symmetric answer-then-critique loop is visibly implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/02-independent-answers-and-critiques/02-02-SUMMARY.md`
</output>
