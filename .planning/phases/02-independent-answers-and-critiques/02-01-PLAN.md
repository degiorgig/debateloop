---
phase: 02-independent-answers-and-critiques
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/debate/prompts.ts
  - src/debate/state.ts
  - src/debate/run-debate.ts
  - src/opencode/models.ts
  - tests/debate/run-debate.test.ts
autonomous: true
requirements:
  - ORCH-01
  - ORCH-02
must_haves:
  truths:
    - "Debater A can generate a real opening answer from the user question."
    - "Debater B generates its opening answer before seeing Debater A's answer."
    - "The run state keeps the opening answers available for later critique stages."
  artifacts:
    - path: "src/debate/prompts.ts"
      provides: "Shared framing block and distinct Debater A/Debater B opening-answer prompt builders"
      exports: ["buildInitialAnswerPrompt"]
    - path: "src/debate/run-debate.ts"
      provides: "SDK-backed execution for the answer_a and answer_b stages"
      exports: ["runDebate"]
    - path: "src/debate/state.ts"
      provides: "Typed in-memory storage for stage results"
      exports: ["DebateStageResult", "DebateRunState"]
  key_links:
    - from: "src/debate/run-debate.ts"
      to: "src/debate/prompts.ts"
      via: "opening-answer stage dispatch"
      pattern: "buildInitialAnswerPrompt"
    - from: "src/debate/run-debate.ts"
      to: "src/opencode/models.ts"
      via: "provider/model ref conversion"
      pattern: "toSdkModelRef"
    - from: "src/debate/run-debate.ts"
      to: "src/debate/state.ts"
      via: "persist answer content in stage results"
      pattern: "result"
---

<objective>
Implement the real independent opening-answer stages.

Purpose: Turn the skeleton into a real two-debater flow while keeping the opening-answer independence guarantee simple and explicit.
Output: Prompt builders, typed stage results, SDK-backed answer generation, and tests proving Debater B stays isolated from Debater A before critique.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-independent-answers-and-critiques/02-CONTEXT.md
@.planning/phases/02-independent-answers-and-critiques/02-RESEARCH.md
@src/debate/run-debate.ts
@src/debate/state.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: add prompt builders and typed stage results for opening answers</name>
  <files>src/debate/prompts.ts, src/debate/state.ts</files>
  <action>Create a focused debate prompt module that defines the shared debate framing block plus clearly distinct role briefs for Debater A and Debater B. Add a typed `DebateStageResult` shape to debate state so completed stages can store content plus minimal execution metadata needed by later critique steps. Keep the module Phase 2-scoped: opening answers and critiques only, no revise or judge prompt work.</action>
  <verify>
    <automated>npm run test -- --run tests/debate/run-debate.test.ts</automated>
    <sampling_rate>run after task completion</sampling_rate>
  </verify>
  <done>The codebase has one shared framing block, two distinct opening role briefs, and a typed place to store answer-stage content in state.</done>
</task>

<task type="auto">
  <name>task 2: execute answer_a and answer_b through isolated OpenCode sessions</name>
  <files>src/debate/run-debate.ts, src/opencode/models.ts</files>
  <action>Extend `runDebate` so `answer_a` and `answer_b` create real OpenCode sessions, send a stage prompt using the acting model, extract only assistant text parts, and store the result on the matching stage state entry. Preserve the fixed stage order from Phase 1, but do not implement revise or judge generation yet. The independence rule is non-negotiable: Debater B's opening answer must be generated from the question, shared framing block, and Debater B brief only, without Debater A's output in the prompt or session history.</action>
  <verify>
    <automated>npm run test -- --run tests/debate/run-debate.test.ts</automated>
    <sampling_rate>run after task completion and before 02-02</sampling_rate>
  </verify>
  <done>Both opening-answer stages produce real model output, the results are stored in state, and Debater B remains blind to Debater A until the critique phase begins.</done>
</task>

</tasks>

<verification>
- `npm run test -- --run tests/debate/run-debate.test.ts` passes.
- Debater A and Debater B opening answers are generated in separate SDK sessions.
- `answer_b` prompt construction excludes Debater A's answer content.
</verification>

<success_criteria>
- Debater A can produce an opening answer for the user question.
- Debater B can produce an independent opening answer before critique begins.
- Opening answers are available in typed state for the next stage group.
</success_criteria>

<output>
After completion, create `.planning/phases/02-independent-answers-and-critiques/02-01-SUMMARY.md`
</output>
